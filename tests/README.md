# Tests

Add automated tests here as the codebase appears.

Prefer a simple split by test type only when it becomes necessary:
- unit
- integration
- end-to-end

Current baseline:
- `tests/Fic.Platform.Web.Tests`
  - xUnit tests around merchant workspace state and loyalty-card date window rules
