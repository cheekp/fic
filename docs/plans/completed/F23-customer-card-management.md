# F23 Customer Card Management

- separated `Operate` from a dedicated `Customers` management surface inside the selected programme
- added explicit customer card statuses so issued passes can be grouped by active, reward-ready, redeemed, scheduled, and expired states
- added proof coverage and validator for customer pass grouping and management actions
