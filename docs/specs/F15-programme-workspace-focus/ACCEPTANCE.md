# F15 Programme Workspace Focus Acceptance

## Acceptance Criteria

1. The active plan is `F15-programme-workspace-focus`, and `F14` has moved to completed plans.
2. Shop settings appear through one clear shop-level action rather than duplicated actions.
3. The left programmes area behaves like a navigation rail with grouped lists and creation, not an explainer panel.
4. The selected-programme banner provides compact context and stops explaining the tab model.
5. Programme configure focuses on rule, schedule, and current customer output with a supporting preview.
6. The updated programme workspace passes targeted component tests.
7. The F15 validator passes.

## Demo Walkthrough

1. Open a merchant workspace and switch to `Programmes`.
2. Confirm the left rail reads as programme navigation and creation only.
3. Confirm the selected-programme header is compact and contextual.
4. Open `Configure` and confirm wallet loyalty card is shown as the current customer output, with the preview still available.
5. Open shop settings and confirm there is only one obvious entrypoint.
6. Run the F15 validator and confirm build plus tests pass.
