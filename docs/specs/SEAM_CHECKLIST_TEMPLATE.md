# Seam Checklist Template

Copy this into a slice requirements doc when the slice spans multiple surfaces or projects.

## Seam Entries

### Seam
- Name:
- Owning context:
- Interface boundary (type + path):
- Surface consumers:
- Boundary tests (file + test names):
- Failure mode if seam breaks:

### Seam
- Name:
- Owning context:
- Interface boundary (type + path):
- Surface consumers:
- Boundary tests (file + test names):
- Failure mode if seam breaks:
